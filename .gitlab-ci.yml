pages:
  stage: deploy
  script:
  - ./scripts/render-page.sh
  cache:
    key: $CI_PROJECT_ID
    paths:
      - data/
  artifacts:
    paths:
    - public
    expire_in: 1 day
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
